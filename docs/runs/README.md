# Pickle runs: one line per run

The reports are evidence kept **on disk** under `Tests/Pickle/Evidence/`, which is ignored by git: captures and
`Player.log` make it grow without limit, and the disk is nearly full. The root `AGENTS.md` ("Test evidence") asks for
the history as one text line per run in this folder, never as folders. `TESTING.md`, "Evidence to keep", says which
files stay and how they are trimmed.

A run is a request dropped with the ticket dispatcher's `Submit-PickleRun.ps1`. `exitReason` is read before the counts,
and the scenario played is compared with the one asked for.

| Request | Played | Pass | Asked for | Revision | exitReason | Scenarios | Verdict |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `20260924-164920-299-ce54` | 2026-09-24 23:12 | English, all DLCs, no optional mod (`sans-facultatifs`) | exploration 1: `::the mod loads and the game holds its four defs` | `bb3c924` | `passed` | 1 of 1, 20.7 s | **Green.** The mod loaded, its four defs were in the game, no error and no warning from the mod. The startup log holds two lines about the test companion and none about the mod: a warning that its dependency on `nelim.crystalball` has no download URL, now fixed, and an error that it "did not load any content", which JoyRescue's log holds in the same form. Evidence: the folder `2026-09-24-explore-1-smoke/` was deleted once the validation passes replayed the scenario, `messages.ndjson` and `report.html` dropped once read. |
| `20260924-164920-839-a1d2` | 2026-09-24 23:14 | English, all DLC, no optional mod (`sans-facultatifs`) | exploration 2: `::sent to the ball and sits beside it` | `bb3c924` | `passed` | 1 of 1, 24.7 s, 11 steps | **Green, first time.** The ball was placed on (146, 156), the anchor itself, with no seat within six cells. The joy giver sent the colonist, who sat on a cell beside it under a `JobDriver_SitFacingBuilding`; the colonist's joy rose in 600 ticks and the job they held was the mod's. No error at all in the scenario and none about the mod in the startup log. The warnings the log does hold come from loading the fixture, saved under an older build, and from the test companion: the dependency URL warning is fixed since this run staged, and the "did not load any content" error is the one of run 1. This settles scenarios 4 and 5 of TESTING.md, which are one scenario. Evidence: the folder `2026-09-24-explore-2-gaze/` was deleted once the validation passes replayed the scenario, the two bulky files dropped once read. |
| `20260924-164921-420-989b` | 2026-09-24 23:17 | French, all DLC, no optional mod (`sans-facultatifs`) | exploration 3: `::the four owned texts read in the language of the pass` | `bb3c924` | `failed` | 0 of 1, 19.2 s | **Red, and the fault is the suite's, not the mod's.** The game started in French and the step read the active language, then refused it: the game names the language folder `French (Français)` and the step compared the whole name with `French`. The launcher's `-Language` is a prefix and the step now treats it as one. Nothing else is in the log but the fixture's warnings and the companion's known lines. The step never reached its comparison of the four texts with the DefInjected file, so that part is still unplayed. A rerun is asked for. Evidence: the folder `2026-09-24-explore-3-french/` was deleted once the validation passes replayed the scenario; `messages.ndjson`, `report.html` and the failure screenshot, 13.9 MB, dropped once read. |
| `20260924-231912-203-f18c` | 2026-09-25 12:38 | French requested, all DLC, no optional mod (`sans-facultatifs`) | fix 1: `::the four owned texts read in the language of the pass`, after the prefix fix | `a085627` | `passed` | 1 of 1, 18.6 s | **Green.** The step recognised the language and compared the four texts with the mod's own resources; no error from the mod, only the fixture's warnings and the companion's known lines. The log does not name the language on a green and both branches of the step can pass, so the step now attaches the language it found; the full French pass will show it. Evidence: the folder `2026-09-24-fix-1-french/` was deleted once the validation passes replayed the scenario, `report.html` and `messages.ndjson` dropped once read. |
| `20260925-124346-826-c70b` | 2026-09-25 14:57 | English, all DLC, no optional mod (`sans-facultatifs`) | validation pass, every scenario, no filter | `f72ecd6` | `failed` | 10 of 11 | **Red on one scenario, and the fault is the suite's.** Ten pass: the four defs, the build, quality, the glow at night, the gaze, the sharing of a ball, tolerance, the blind and the deaf, the four texts (the step attached `English`) and the inspect capture. The save scenario failed after the reload: "the joy giver offered Save-3 nothing: CanBeGivenTo holds but TryGiveJob returned no job". The two colonists that were gazing when the game was saved still hold the ball after the reload, and it takes two, so a third is rightly offered nothing: the scenario asked the giver for a job the ball's own cap forbids. The scenario now asserts that the pair still gazes and the third is offered nothing. The `@review` captures were opened: the ball in the inspect pane reads "Crystal ball (normal)", quality Normal, no raw key; the pane shows the name and not the description, which sits behind the info card and is not captured. At night the ball is lit (the ground reads "Lit (50%)") and the glow is visible. Evidence in `2026-09-25-validation-english/`, `report.html`, `messages.ndjson` and the failure capture dropped once read. |
| `20260925-124349-582-3f8c` | 2026-09-25 15:02 | French, all DLC, no optional mod (`sans-facultatifs`) | validation pass, every scenario, no filter | `f72ecd6` | `failed` | 10 of 11 | **Red on the same scenario, for the same reason as the English pass.** Ten pass, the four texts among them (the step attached `French (Français)`, the language the pass was staged in). The inspect capture reads "Boule de cristal (normal)", "Qualité : Normal", no raw key and no overflow; as in English the description is not in the pane. Evidence in `2026-09-25-validation-french/`, `report.html`, `messages.ndjson`, the failure capture and the night capture (language independent, kept in the English folder) dropped once read. |
| `20260925-153544-664-01c7` | 2026-09-25 14:45 | English, all DLC, no optional mod (`sans-facultatifs`) | fix 2: `::a save taken with two colonists gazing`, the corrected save scenario | `8ecaf70` | `passed` | 1 of 1, 22.8 s | **Green.** The game saved with two colonists gazing and reloaded with no error, the masterwork ball stood at (146, 156) with its quality, the two colonists still gazed at it, and the third was offered nothing. Only the fixture's warnings and the companion's known line in the log. This closes the eleven scenarios: each has a green report on the current suite, ten from the two full passes (`f72ecd6`, the suite of those ten unchanged since) and this one. Evidence in `2026-09-25-fix-2-save/`, `report.html` and `messages.ndjson` dropped once read. |
| `20260925-184945-472-5abc` | 2026-09-25 21:15 | English, all DLC, no optional mod (`sans-facultatifs`) | exploration 5: `05-workshop-captures`, the three close-ups for the Workshop gallery | `2a39367` | `passed` | 3 of 3, 29.8 s | **Green, and the pictures are weak.** The route ran: the ball by day, at night, and a colonist sitting beside it. The game was already at its closest zoom (about 46 pixels a cell, the same as the review captures), so the ball stays about 30 pixels wide; cropped to 480 x 270 around it, the fixture's zone tints show behind it, and in the third the colonist's name label covers the ball. Superseded by the run below, on the zen studio; its evidence folder was deleted. |
| `20260925-214326-453-a0c6` | 2026-09-25 23:07 | English, pass `workshop` (PickleTools' screenshot studio), no optional mod | exploration 6: `05-workshop-captures` on the Nelim zen meadow studio | `768ae50` | `passed` | 3 of 3, 36.2 s | **Green, and the pictures are clean.** Same three close-ups on the studio's flower glade with the game's screenshot mode on: no interface, no zone tints. The ball is placed at (154, 98) and Miel, the studio's own colonist, sits beside it. Still native scale, the ball about 30 pixels wide; at night the glow is faint and the colonist's name label touches the ball. The startup log holds the known "did not load any content" lines for the test companion and for the studio. Captures kept in `2026-09-25-workshop-studio/screenshots/` until Virginie decides on the crops; `report.html` and `messages.ndjson` dropped once read. |
| `20260926-004239-900-124f` | 2026-09-26 | English, pass `workshop` (PickleTools' screenshot studio), no optional mod | exploration 7: `05-workshop-captures`, Miel dressed in a robe | `9abc18e` | `passed` | 3 of 3, 48.6 s | **Green.** Same three close-ups; Miel now wears a robe (grey-blue, visible in the third). Ball still about 30 pixels wide, discreet glow at night, label touching the ball. Supersedes `a0c6`, whose evidence folder was deleted. Captures kept in `2026-09-26-workshop-robe/screenshots/` until Virginie decides; `report.html` and `messages.ndjson` dropped. |

<!-- 2026-09-26: gallery kept by Virginie; dry-run 36232421213 green on 7d64a56491d7f131ec5770342f65875d993f8279; publish run 36232486752 waiting for her approval of steam-production. -->
| `20260926-220942-644-145b` | 2026-09-26 | English, all DLC, no optional mod | exploration 8: `::the ball's description on its info card` | `b7ac566` | `passed` | 1 of 1 | **Green, and the description fits.** The info card of the ball opens and lays out the description of the def in full (two paragraphs, no overflow, no raw key), beside the recreation type Divination, the cost of 40 jade and 5 gold and the source Crystal Ball. The "Description" row of the table stays empty, as it does for every def: the text sits in the right-hand pane. Closes the `remaining` item on the info card. Capture kept in `2026-09-26-info-card/screenshots/`; `report.html` and `messages.ndjson` dropped. |
| `20260926-221303-467-d421` | 2026-09-26 | English, all DLC, no optional mod (`sans-facultatifs`) | non-regression of the published 1.0.0, every scenario, no filter | `b400a22` (Mod as published `7d64a56`) | `passed` | 12 of 15, 3 skipped | **Green.** The twelve scenarios of the four first features pass, the info card one included. The three skipped are the `@requires:nelim.pickletools.screenshotstudio` ones of `05-workshop-captures.feature`, played in their own `workshop` pass (`0f99`). Evidence in `2026-09-26-nonreg-english/`, `report.html` and `messages.ndjson` dropped. |
| `20260926-221303-762-0f99` | 2026-09-26 | English, pass `workshop` (PickleTools' screenshot studio) | non-regression of the published 1.0.0, `05-workshop-captures` | `b400a22` (Mod as published `7d64a56`) | `passed` | 3 of 3 | **Green.** The route of the three Workshop pictures still runs. The captures were not kept: the pictures on the Steam page are the ones of run `124f`, committed in `Art/WorkshopScreenshots/`. `report.html`, `messages.ndjson` and the raw screenshots dropped. |
